//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

package com.example.sportsmanagementappforcoach;

import java.io.IOException;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.scene.control.Button;

public class CreateProgramPage {
    import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.DialogPane;
import javafx.scene.control.MenuButton;
import javafx.scene.control.MenuItem;

    public class PleaseProvideControllerClassName {

        @FXML
        private MenuItem Archery;

        @FXML
        private MenuItem Badminton;

        @FXML
        private MenuItem Baseball;

        @FXML
        private MenuItem Basketball;

        @FXML
        private MenuItem Boxing;

        @FXML
        private MenuItem Chess;

        @FXML
        private MenuButton Choose;

        @FXML
        private Button Creat_Program;

        @FXML
        private MenuItem Cricket;

        @FXML
        private MenuItem Cycling;

        @FXML
        private MenuItem Football;

        @FXML
        private MenuItem Golf;

        @FXML
        private MenuItem Hockey;

        @FXML
        private Button Next;

        @FXML
        private MenuItem Table_Tennis;

        @FXML
        private DialogPane Title;

        @FXML
        private DialogPane Title1;

        @FXML
        private MenuItem Volleyball;


    public CreateProgramPage() {
    }

    public void Create_ProgramButtonPressed(ActionEvent event) throws IOException {
        SceneController sceneController = new SceneController();
        sceneController.SwitchToCreate_Page(event);
    }

    public void NextButtonPressed(ActionEvent event) throws IOException {
        SceneController sceneController = new SceneController();
        sceneController.SwitchToNextPage(event);
    }
}
